apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-kubernetes_argocd
  namespace: slidev
  labels:
    name: ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  tls:
    - hosts:
        - slidev.tingtt.jp
      secretName: ingress-nginx-tls-secret
  rules:
    - host: slidev.tingtt.jp
      http:
        paths:
          - path: "/kubernetes_argocd/(.*)"
            pathType: Prefix
            backend:
              service:
                name: slidev-kubernetes_argocd
                port:
                  number: 80
          - path: "/kubernetes_argocd"
            pathType: Exact
            backend:
              service:
                name: slidev-kubernetes_argocd
                port:
                  number: 80